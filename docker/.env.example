# OrbitX Selenium Automation Environment variables
# Copy this to .env in the docker/ directory before running docker-compose

# Target URL of the application under test (e.g. mock server or live environment)
BASE_URL=http://localhost:8080

# Credentials used to authenticate
USERNAME=astronaut@orbitx.com
PASSWORD=password123
