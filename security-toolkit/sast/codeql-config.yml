name: "OrbitX CodeQL Custom Config"

# Exclude third-party node packages and testing automation scripts from main security scans
paths-ignore:
  - "**/node_modules/**"
  - "**/venv/**"
  - "**/automation/**"
  - "**/deployment-validation/**"

queries:
  - uses: security-extended
  - uses: security-and-quality
