# OrbitX Test Reports Output Directory

This directory receives generated test execution outputs, including:
- JUnit XML reports (`backend-junit.xml`, `playwright-junit.xml`, `mobile-junit.xml`)
- Playwright HTML reports (`playwright-report/`)
- Screenshot artifacts from mobile/web failures (`screenshots/`)
- k6 performance log outputs
